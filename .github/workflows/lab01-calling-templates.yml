name: Lab01 Calling Templates
run-name: Lab01 - ${{ github.actor }} is testing out GitHub Actions 🚀
permissions: read-all
on:
  workflow_dispatch:
jobs:
  example-job:
    runs-on: ubuntu-latest
    steps:
      - name: Run hello world
        run: echo "🎉 Step of the job is running on ${{ runner.os }}!"
